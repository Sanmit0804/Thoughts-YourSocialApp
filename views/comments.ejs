<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Page</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700&family=Roboto:wght@400;500;700&display=swap');
        body {
            font-family: 'Roboto', sans-serif;
            -ms-overflow-style: none; /* for Internet Explorer, Edge */
            scrollbar-width: none; /* for Firefox */
            overflow-y: scroll;
        }
        body::-webkit-scrollbar {
            display: none; /* for Chrome, Safari, and Opera */
        }
        h1, h5 {
            font-family: 'Roboto', serif;
        }
        .hidden {
            display: none;
        }
    </style>
</head>
<body class="bg-white text-black">
    <div class="p-4 flex justify-between items-center bg-gray-100 shadow-md">
        <h1 class="text-3xl font-bold"><a href="/home">Home</a></h1>
        <a href="/logout" class="text-black hover:text-gray-600 transition-colors">Logout</a>
    </div>

    <div class="container mx-auto p-6">
        <div class="bg-gray-100 p-6 rounded-lg shadow-md mb-8">
            <div class="flex items-center m-1">
                <img src="/image/uploads/<%=post.user.profilePic %>" alt="Profile Picture" class="w-5 h-5 rounded-full object-cover ">
                <h3 class="text-lg font-bold ml-2 "><%= post.user.username %></h3>
            </div>
            <p class="text-gray-600 mb-4"><%= post.content %></p>
            <div class="flex space-x-4">
                <small><%= post.likes.length %> ❤️</small>
                <% if(post.likes.indexOf(post.user._id)==-1) { %>
                <a href="/like/<%= post._id%>" class="text-gray-600 hover:text-pink-400">Like</a>
                <% } else { %>
                <a href="/like/<%= post._id%>" class="text-gray-600 hover:text-pink-400">Dislike</a>
                <% } %>
            </div>

            <!-- Comments Section -->
            <div class="mt-4">
                <% post.comments.slice(0).forEach(comment => { %>
                <div class="bg-white p-2 rounded-lg shadow-md mb-2">
                    <div class="flex items-center mb-1">
                        <img src="/image/uploads/<%= comment.createdBy.profilePic %>" alt="Profile Picture" class="w-4 h-4 rounded-full object-cover">
                        <h5 class="text-sm font-bold ml-2"><%= comment.createdBy.username %></h5>
                    </div>
                    <p class="text-gray-600"><%= comment.content %></p>
                </div>
                <% }) %>
            </div>
        </div>

        <h5 class="text-xl font-bold mb-6">Add a Comment</h5>
        <form action="/comments/<%= post._id %>" method="post" class="mb-8">
            <textarea
                name="content"
                placeholder="Write your comment here...💬"
                class="w-full p-4 bg-gray-100 text-black border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-gray-400 focus:border-transparent mb-4"
            ></textarea>
            <button
                type="submit"
                class="py-2 px-6 bg-black text-white font-bold rounded-md shadow-md hover:bg-gray-800 focus:outline-none focus:ring-2 focus:ring-gray-400 focus:ring-opacity-75 transition-colors">
                Submit
            </button>
        </form>
    </div>
</body>
</html>
