<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload Profile Picture</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700&family=Roboto:wght@400;500;700&display=swap');
        body {
            font-family: 'Roboto', sans-serif;
        }
        h1, h3 {
            font-family: 'Roboto', serif;
        }
    </style>
</head>
<body class="bg-white text-black">
    <div class="p-4 flex justify-between items-center bg-gray-100 shadow-md">
        <a href="/profile"><h1 class="text-3xl font-bold">Profile</h1></a>
        <a href="/logout" class="text-black hover:text-gray-600 transition-colors">Logout</a>
    </div>

    <div class="container mx-auto p-6 max-w-2xl">
        <h3 class="text-2xl font-bold mb-6">Your Profile</h3>
        <form autocomplete="off" action="/upload" method="post" enctype="multipart/form-data" class="bg-gray-100 p-8 rounded-lg shadow-lg">
            <div class="mb-6">
                <label for="image" class="block text-gray-600 mb-2">Upload Profile Picture</label>
                <input type="file" name="image" id="image" class="w-full text-black bg-white border border-gray-300 rounded-md p-3 cursor-pointer focus:outline-none focus:ring-2 focus:ring-gray-400 focus:border-transparent">
            </div>
          
            <div class="mb-4">
                <label for="name" class="block text-sm font-medium text-gray-600">Name</label>
                <input type="text" id="name" name="name" value="<%= user.name %>" class="mt-1 block w-full px-3 py-2 bg-white text-black border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-gray-400 focus:border-transparent" />
            </div>
        
            <div class="flex justify-end">
                <input type="submit" value="Update" class="py-2 px-6 bg-black text-white font-bold rounded-md shadow-md hover:bg-gray-800 focus:outline-none focus:ring-2 focus:ring-gray-400 focus:ring-opacity-75 cursor-pointer transition-colors">
            </div>
        </form>
    </div>
</body>
</html>
